from setuptools import setup

setup(name='toy-hook',
      version='0.1',
      description='toy hook that prints a message on firing',
      url='http://github.com/pvtodorov/toy_hook',
      author='Petar Todorov',
      license='MIT',
      packages=['toy_hook'],
      zip_safe=False)